[Unit]
Description=Pi Zero Camera RTSP Stream (rpicam-vid -> ffmpeg -> MediaMTX)
After=network.target docker.service
Requires=docker.service

[Service]
Type=simple
ExecStart=/usr/local/bin/cam-stream.sh
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
